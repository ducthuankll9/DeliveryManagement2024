
@{
    ViewBag.Title = "Xem trước bản in";
    Layout = "~/Areas/StationStaffs/Views/Shared/_CommonLayout.cshtml";

    var listPdfPaths = ViewBag.ListPaths as List<string>;
}

<h2>Xem trước bản in</h2>

@if (listPdfPaths != null)
{
    foreach (var path in listPdfPaths)
    {
        <div>
            <!--<object data="@path" type="application/pdf">
                <p>Trình duyệt của bạn không hỗ trợ xem file PDF. Bạn có thể tải file PDF xuống để xem.</p>
            </object>-->
            <embed src="@path" style="width:100% ; height:700px;" />
        </div>
        <hr />
    }
}